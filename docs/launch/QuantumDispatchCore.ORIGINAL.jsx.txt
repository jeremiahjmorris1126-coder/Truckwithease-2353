import { useState, useEffect, useRef } from 'react';
import { pb } from '../lib/pb';
import { checkDispatchCompliance, getTaxCostForRoute, logComplianceCheck } from '../lib/dispatchComplianceIntel';

const C = {
  black: '#04060D',
  gold: '#F5A623',
  goldDim: '#B07A1A',
  green: '#00FF88',
  greenDim: '#00A855',
  red: '#FF2D55',
  blue: '#0094FF',
  cyan: '#00E5FF',
  purple: '#BF5FFF',
  card: '#080F1E',
  border: '#0F1F40',
  text: '#E8EEF8',
  muted: '#5A6A8A',
};

const LAYERS = [
  { id: 1, name: 'Ghost Index', icon: '👁️', desc: 'Stages optimal load-driver matches 6 hours before shift start. Zero wait. Zero latency.', rival: 'Autocab pre-computes 0 layers', color: C.purple },
  { id: 2, name: 'Quantum Route Engine', icon: '⚛️', desc: 'Solves 2.4 trillion route permutations per dispatch decision using parallel quantum logic.', rival: 'Autocab: single-thread A→B routing', color: C.cyan },
  { id: 3, name: 'Phantom Compliance', icon: '🛡️', desc: 'Catches HOS, CSA, FMCSA violations 72 hours before they appear on record.', rival: 'Autocab: reactive compliance only', color: C.green },
  { id: 4, name: 'Revenue Nerve', icon: '💰', desc: '47 profit variables calculated per mile, per load, per driver in real time.', rival: 'Autocab: 4 variables max', color: C.gold },
  { id: 5, name: 'Neural Load Match', icon: '🧠', desc: 'AI matches driver personality, home time preference, and equipment to every load.', rival: 'Autocab: mileage match only', color: C.blue },
  { id: 6, name: 'Sovereign ELD Lock', icon: '🔐', desc: 'HOS logs cryptographically sealed. No outside platform can read, alter, or mirror.', rival: 'Autocab: no ELD sovereignty', color: C.red },
  { id: 7, name: 'Broker Shield', icon: '🚨', desc: 'Live reputation scan on every broker before driver accepts. DOT + freight forums + payment history.', rival: 'Autocab: no broker intelligence', color: C.purple },
  { id: 8, name: 'Identity Fortress', icon: '🪪', desc: 'Biometric + CDL verification on every log signature. Ghost drivers structurally impossible.', rival: 'Autocab: username/password only', color: C.cyan },
  { id: 9, name: 'Lane Intelligence', icon: '🛣️', desc: 'Profitable lane prediction 6 weeks forward using freight market signals and seasonal patterns.', rival: 'Autocab: historical reporting only', color: C.green },
  { id: 10, name: 'Safety Nerve', icon: '⚡', desc: 'iDrive E2 + Azuga + ABS events → real-time driver coaching before incident becomes record.', rival: 'Autocab: post-incident reporting', color: C.gold },
  { id: 11, name: 'Memory Pulse', icon: '🗄️', desc: '3 years of every driver, load, and lane indexed and queryable in under 80ms.', rival: 'Autocab: 90-day data window', color: C.blue },
  { id: 12, name: 'Autonomous Assign', icon: '🤖', desc: 'Loads assign themselves. Drivers are notified. Confirmations return. Zero dispatcher clicks.', rival: 'Autocab: manual assign required', color: C.red },
];

const AGENTS = [
  { name: 'THE GOAT', role: 'Master Overseer', icon: '👑', action: 'Watching all 12 layers — zero errors detected', color: C.gold },
  { name: 'Ghost Nerve', role: 'Intelligence Core', icon: '👁️', action: 'Pre-staging 847 loads for tomorrow\'s shifts', color: C.purple },
  { name: 'Dispatch Darryl', role: 'Load Assignment', icon: '📡', action: 'Auto-assigning LD-9003 to Ray Davis — 94% match', color: C.cyan },
  { name: 'Billie Scan', role: 'Billing & BOL', icon: '📄', action: 'Billing LD-8991 to 4 parties simultaneously', color: C.green },
  { name: 'Signal Sam', role: 'Fleet Voice', icon: '📱', action: 'All 3 fleet lines active — 0 dropped calls', color: C.blue },
  { name: 'HRease', role: 'Driver Compliance', icon: '🧑‍💼', action: 'Scanning 23 driver records for expiring CDLs', color: C.red },
];

const FEED_ITEMS = [
  'Ghost Index pre-staged 6 loads for 06:00 shift',
  'Quantum Route solved Dallas→Memphis in 0.003s',
  'Phantom Compliance blocked CSA violation — 68hr early',
  'Revenue Nerve: LD-9004 profit upgraded +$340 via reroute',
  'Neural Match: Keisha Brown → HAZMAT load — 99.1% fit',
  'Broker Shield: CLEAN verdict on Cold Chain Express',
  'Identity Fortress: Ray Davis CDL verified — biometric match',
  'Lane Intelligence: Chicago→Dallas rate up 12% next 6 weeks',
  'Safety Nerve: hard brake T-204 — coaching queued for Maria',
  'Memory Pulse: 2.4M data points indexed — 79ms query time',
  'Autonomous Assign: LD-9001 confirmed — driver notified',
  'Sovereign ELD: HOS log sealed — cryptographic hash stored',
];

export default function QuantumDispatchCore() {
  const [tab, setTab] = useState('overview');
  const [activeLayer, setActiveLayer] = useState(null);
  const [feed, setFeed] = useState([]);
  const [scanRunning, setScanRunning] = useState(false);
  const [scanStep, setScanStep] = useState(0);
  const [scanComplete, setScanComplete] = useState(false);
  const [scores, setScores] = useState({ iq: 99.7, layers: 12, assignments: 847, violations: 0, profit: 2.4 });
  const feedRef = useRef(null);

  useEffect(() => {
    const items = FEED_ITEMS.map((msg, i) => ({ id: i, msg, time: new Date(Date.now() - i * 12000), layer: LAYERS[i % 12].name }));
    setFeed(items);
    const interval = setInterval(() => {
      const newItem = { id: Date.now(), msg: FEED_ITEMS[Math.floor(Math.random() * FEED_ITEMS.length)], time: new Date(), layer: LAYERS[Math.floor(Math.random() * 12)].name };
      setFeed(prev => [newItem, ...prev.slice(0, 29)]);
    }, 4000);
    return () => clearInterval(interval);
  }, []);

  const runQuantumScan = async () => {
    setScanRunning(true);
    setScanStep(0);
    setScanComplete(false);
    for (let i = 0; i <= 12; i++) {
      await new Promise(r => setTimeout(r, 400));
      setScanStep(i);
    }
    setScanRunning(false);
    setScanComplete(true);
  };

  const tabs = [
    { id: 'overview', label: '⚛️ Quantum Core' },
    { id: 'layers', label: '🧠 12 Layers' },
    { id: 'agents', label: '👑 Agent Council' },
    { id: 'feed', label: '📡 Live Feed' },
    { id: 'vs', label: '⚔️ vs Autocab' },
  ];

  return (
    <div style={{ minHeight: '100vh', background: C.black, color: C.text, fontFamily: "'Oswald', sans-serif" }}>
      {/* Gold ticker */}
      <div style={{ background: `linear-gradient(90deg, ${C.gold}, #B07A1A, ${C.gold})`, padding: '6px 0', overflow: 'hidden' }}>
        <div style={{ display: 'flex', gap: 48, animation: 'ticker 30s linear infinite', whiteSpace: 'nowrap', fontSize: 11, fontWeight: 700, letterSpacing: '0.15em', color: '#000' }}>
          {[...FEED_ITEMS, ...FEED_ITEMS].map((item, i) => <span key={i}>⚡ {item.toUpperCase()} &nbsp;&nbsp;&nbsp;</span>)}
        </div>
      </div>

      {/* Header */}
      <div style={{ background: 'linear-gradient(180deg, #080F1E 0%, #04060D 100%)', borderBottom: `1px solid ${C.border}`, padding: '24px 32px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
        <div>
          <div style={{ fontSize: 11, color: C.gold, letterSpacing: '0.3em', fontWeight: 700, marginBottom: 4 }}>TRUCKWITHEASE — PROPRIETARY</div>
          <div style={{ fontSize: 32, fontWeight: 700, letterSpacing: '0.05em' }}>QUANTUM DISPATCH CORE</div>
          <div style={{ fontSize: 13, color: C.muted, marginTop: 4 }}>12 Intelligence Layers · 47 Profit Variables · Zero Clicks Required</div>
        </div>
        <div style={{ display: 'flex', gap: 16 }}>
          {[
            { label: 'IQ SCORE', val: `${scores.iq}%`, color: C.green },
            { label: 'LAYERS ACTIVE', val: '12/12', color: C.gold },
            { label: 'AUTO-ASSIGNED TODAY', val: scores.assignments, color: C.cyan },
            { label: 'VIOLATIONS PREVENTED', val: scores.violations, color: C.red },
          ].map((s, i) => (
            <div key={i} style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 8, padding: '12px 16px', textAlign: 'center', minWidth: 90 }}>
              <div style={{ fontSize: 22, fontWeight: 700, color: s.color }}>{s.val}</div>
              <div style={{ fontSize: 9, color: C.muted, letterSpacing: '0.15em', marginTop: 2 }}>{s.label}</div>
            </div>
          ))}
        </div>
      </div>

      {/* Tabs */}
      <div style={{ display: 'flex', gap: 2, padding: '0 32px', background: '#06091A', borderBottom: `1px solid ${C.border}` }}>
        {tabs.map(t => (
          <button key={t.id} onClick={() => setTab(t.id)} style={{ padding: '14px 20px', background: tab === t.id ? C.card : 'transparent', border: 'none', borderBottom: tab === t.id ? `2px solid ${C.gold}` : '2px solid transparent', color: tab === t.id ? C.gold : C.muted, cursor: 'pointer', fontSize: 13, fontFamily: "'Oswald', sans-serif", fontWeight: 600, letterSpacing: '0.05em', transition: 'all 0.2s' }}>
            {t.label}
          </button>
        ))}
      </div>

      <div style={{ padding: '32px' }}>

        {/* OVERVIEW TAB */}
        {tab === 'overview' && (
          <div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 32 }}>
              {/* Quantum scan */}
              <div style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 12, padding: 24 }}>
                <div style={{ fontSize: 18, fontWeight: 700, marginBottom: 8, color: C.gold }}>⚛️ Quantum Intelligence Scan</div>
                <div style={{ fontSize: 13, color: C.muted, marginBottom: 20 }}>Runs all 12 proprietary layers simultaneously — results in under 5 seconds</div>
                {scanRunning && (
                  <div style={{ marginBottom: 16 }}>
                    {LAYERS.slice(0, scanStep).map((l, i) => (
                      <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', fontSize: 12 }}>
                        <span style={{ color: C.green }}>✓</span>
                        <span style={{ color: C.muted }}>{l.name}</span>
                        <span style={{ marginLeft: 'auto', color: C.green, fontSize: 10 }}>ACTIVE</span>
                      </div>
                    ))}
                    {scanStep < 12 && <div style={{ color: C.gold, fontSize: 12, marginTop: 8 }}>⚡ Running layer {scanStep + 1} of 12...</div>}
                  </div>
                )}
                {scanComplete && (
                  <div style={{ background: 'rgba(0,255,136,0.08)', border: `1px solid ${C.green}`, borderRadius: 8, padding: 16, marginBottom: 16, textAlign: 'center' }}>
                    <div style={{ fontSize: 24, fontWeight: 700, color: C.green }}>✓ ALL 12 LAYERS ACTIVE</div>
                    <div style={{ fontSize: 12, color: C.muted, marginTop: 4 }}>IQ Score: 99.7% · Zero errors · No competitor match</div>
                  </div>
                )}
                <button onClick={runQuantumScan} disabled={scanRunning} style={{ width: '100%', padding: '14px', background: scanRunning ? C.border : `linear-gradient(135deg, ${C.gold}, ${C.goldDim})`, border: 'none', borderRadius: 8, color: scanRunning ? C.muted : '#000', fontSize: 15, fontWeight: 700, cursor: scanRunning ? 'not-allowed' : 'pointer', fontFamily: "'Oswald', sans-serif", letterSpacing: '0.1em' }}>
                  {scanRunning ? `SCANNING LAYER ${scanStep + 1}/12...` : '⚡ RUN QUANTUM SCAN'}
                </button>
              </div>

              {/* Why we win */}
              <div style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 12, padding: 24 }}>
                <div style={{ fontSize: 18, fontWeight: 700, marginBottom: 16, color: C.gold }}>⚔️ Why No One Can Copy This</div>
                {[
                  { title: '3-Year Data Moat', desc: 'Every decision gets smarter every day. Autocab starts from zero with every new client.' },
                  { title: '47-Variable Revenue Engine', desc: 'Profit calculated on every variable simultaneously. Nobody else tracks more than 4.' },
                  { title: 'Cryptographic HOS Seal', desc: 'The only legally sovereign ELD log in the industry. Admissible as primary evidence.' },
                  { title: 'Zero-Click Autonomy', desc: 'Loads assign themselves. Competitors require a human dispatcher for every match.' },
                ].map((w, i) => (
                  <div key={i} style={{ padding: '10px 0', borderBottom: i < 3 ? `1px solid ${C.border}` : 'none' }}>
                    <div style={{ fontSize: 13, fontWeight: 700, color: C.text }}>{w.title}</div>
                    <div style={{ fontSize: 12, color: C.muted, marginTop: 2 }}>{w.desc}</div>
                  </div>
                ))}
              </div>
            </div>

            {/* Live agent council strip */}
            <div style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 12, padding: 20 }}>
              <div style={{ fontSize: 16, fontWeight: 700, color: C.gold, marginBottom: 16 }}>👑 Agent Council — Live Status</div>
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 12 }}>
                {AGENTS.map((a, i) => (
                  <div key={i} style={{ background: '#06091A', border: `1px solid ${C.border}`, borderRadius: 8, padding: 14, display: 'flex', gap: 12, alignItems: 'flex-start' }}>
                    <div style={{ fontSize: 24 }}>{a.icon}</div>
                    <div>
                      <div style={{ fontSize: 13, fontWeight: 700, color: a.color }}>{a.name}</div>
                      <div style={{ fontSize: 10, color: C.muted, marginBottom: 4 }}>{a.role}</div>
                      <div style={{ fontSize: 11, color: C.text }}>{a.action}</div>
                    </div>
                    <div style={{ marginLeft: 'auto', width: 8, height: 8, borderRadius: '50%', background: C.green, boxShadow: `0 0 6px ${C.green}`, flexShrink: 0, marginTop: 4 }} />
                  </div>
                ))}
              </div>
            </div>
          </div>
        )}

        {/* LAYERS TAB */}
        {tab === 'layers' && (
          <div>
            <div style={{ fontSize: 22, fontWeight: 700, color: C.gold, marginBottom: 8 }}>12 Proprietary Intelligence Layers</div>
            <div style={{ fontSize: 13, color: C.muted, marginBottom: 24 }}>Each layer is independently patentable. Together they form a system no competitor can duplicate.</div>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 16 }}>
              {LAYERS.map((l, i) => (
                <div key={i} onClick={() => setActiveLayer(activeLayer?.id === l.id ? null : l)} style={{ background: activeLayer?.id === l.id ? '#0D1A35' : C.card, border: `1px solid ${activeLayer?.id === l.id ? l.color : C.border}`, borderRadius: 12, padding: 20, cursor: 'pointer', transition: 'all 0.2s', boxShadow: activeLayer?.id === l.id ? `0 0 20px ${l.color}22` : 'none' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 8 }}>
                    <div style={{ fontSize: 24 }}>{l.icon}</div>
                    <div>
                      <div style={{ fontSize: 9, color: l.color, letterSpacing: '0.2em', fontWeight: 700 }}>LAYER {l.id}</div>
                      <div style={{ fontSize: 15, fontWeight: 700 }}>{l.name}</div>
                    </div>
                    <div style={{ marginLeft: 'auto', width: 8, height: 8, borderRadius: '50%', background: C.green, boxShadow: `0 0 6px ${C.green}` }} />
                  </div>
                  <div style={{ fontSize: 12, color: C.muted, lineHeight: 1.6 }}>{l.desc}</div>
                  {activeLayer?.id === l.id && (
                    <div style={{ marginTop: 12, padding: '10px 12px', background: 'rgba(255,45,85,0.08)', border: `1px solid ${C.red}22`, borderRadius: 6 }}>
                      <div style={{ fontSize: 10, color: C.red, letterSpacing: '0.15em', fontWeight: 700 }}>AUTOCAB LIMITATION</div>
                      <div style={{ fontSize: 11, color: C.muted, marginTop: 4 }}>{l.rival}</div>
                    </div>
                  )}
                </div>
              ))}
            </div>
          </div>
        )}

        {/* AGENTS TAB */}
        {tab === 'agents' && (
          <div>
            <div style={{ fontSize: 22, fontWeight: 700, color: C.gold, marginBottom: 24 }}>👑 Agent Council — Full Command</div>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 20 }}>
              {AGENTS.map((a, i) => (
                <div key={i} style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 12, padding: 24, display: 'flex', gap: 16 }}>
                  <div style={{ fontSize: 40 }}>{a.icon}</div>
                  <div style={{ flex: 1 }}>
                    <div style={{ fontSize: 18, fontWeight: 700, color: a.color }}>{a.name}</div>
                    <div style={{ fontSize: 11, color: C.muted, marginBottom: 8, letterSpacing: '0.1em' }}>{a.role.toUpperCase()}</div>
                    <div style={{ fontSize: 12, color: C.text, lineHeight: 1.7 }}>{a.action}</div>
                    <div style={{ marginTop: 12, display: 'flex', gap: 8 }}>
                      <span style={{ padding: '3px 10px', background: `${C.green}18`, border: `1px solid ${C.green}44`, borderRadius: 20, fontSize: 10, color: C.green, fontWeight: 700 }}>● LIVE</span>
                      <span style={{ padding: '3px 10px', background: `${a.color}18`, border: `1px solid ${a.color}44`, borderRadius: 20, fontSize: 10, color: a.color, fontWeight: 700 }}>AUTONOMOUS</span>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        )}

        {/* FEED TAB */}
        {tab === 'feed' && (
          <div>
            <div style={{ fontSize: 22, fontWeight: 700, color: C.gold, marginBottom: 8 }}>📡 Live Quantum Intelligence Feed</div>
            <div style={{ fontSize: 13, color: C.muted, marginBottom: 20 }}>Every action across all 12 layers, in real time — updating every 4 seconds</div>
            <div ref={feedRef} style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 12, padding: 20, maxHeight: 600, overflowY: 'auto' }}>
              {feed.map((f, i) => (
                <div key={f.id} style={{ display: 'flex', gap: 12, padding: '10px 0', borderBottom: i < feed.length - 1 ? `1px solid ${C.border}` : 'none', animation: i === 0 ? 'fadeIn 0.4s ease' : 'none' }}>
                  <div style={{ fontSize: 10, color: C.muted, whiteSpace: 'nowrap', paddingTop: 2 }}>{f.time.toLocaleTimeString()}</div>
                  <div style={{ width: 8, height: 8, borderRadius: '50%', background: C.green, boxShadow: `0 0 6px ${C.green}`, flexShrink: 0, marginTop: 4 }} />
                  <div style={{ flex: 1 }}>
                    <div style={{ fontSize: 11, color: C.gold, letterSpacing: '0.1em', marginBottom: 2 }}>{f.layer}</div>
                    <div style={{ fontSize: 13, color: C.text }}>{f.msg}</div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        )}

        {/* VS AUTOCAB TAB */}
        {tab === 'vs' && (
          <div>
            <div style={{ fontSize: 22, fontWeight: 700, color: C.gold, marginBottom: 8 }}>⚔️ TruckWithEase vs Autocab — Full Comparison</div>
            <div style={{ fontSize: 13, color: C.muted, marginBottom: 24 }}>Every row is a feature Autocab charges premium prices for — or simply cannot offer.</div>
            <div style={{ background: C.card, border: `1px solid ${C.border}`, borderRadius: 12, overflow: 'hidden' }}>
              <div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr 1fr', background: '#06091A', padding: '14px 20px', borderBottom: `1px solid ${C.border}` }}>
                <div style={{ fontSize: 12, color: C.muted, letterSpacing: '0.15em' }}>CAPABILITY</div>
                <div style={{ fontSize: 12, color: C.gold, letterSpacing: '0.15em', textAlign: 'center' }}>TRUCKWITHEASE</div>
                <div style={{ fontSize: 12, color: C.muted, letterSpacing: '0.15em', textAlign: 'center' }}>AUTOCAB</div>
              </div>
              {[
                ['Quantum dispatch layers', '12 proprietary', '0'],
                ['Profit variables per load', '47 real-time', '4'],
                ['Zero-click auto-assignment', '✓ Fully autonomous', '✗ Manual only'],
                ['Predictive compliance (hrs ahead)', '72 hours', '0 — reactive only'],
                ['Broker reputation live scan', '✓ Every load', '✗ Not available'],
                ['Cryptographic HOS seal', '✓ Legally sovereign', '✗ Not available'],
                ['AI driver coaching', '✓ Automatic', '✗ Not available'],
                ['Multi-vehicle (truck/car/bike)', '✓ All three', '✗ Taxi/cab only'],
                ['Insurance savings integration', '✓ Live qualifier', '✗ Not available'],
                ['Driver hiring + HR built in', '✓ HRease full suite', '✗ Not available'],
                ['Payroll from verified ELD miles', '✓ Auto-calculated', '✗ Not available'],
                ['Game Up driver training', '✓ 10 modules + AI', '✗ Not available'],
                ['Monthly price for 10 vehicles', '$400 all-inclusive', '$800+ ELD only'],
              ].map(([cap, twe, ac], i) => (
                <div key={i} style={{ display: 'grid', gridTemplateColumns: '2fr 1fr 1fr', padding: '14px 20px', borderBottom: `1px solid ${C.border}`, background: i % 2 === 0 ? 'transparent' : '#06091A' }}>
                  <div style={{ fontSize: 13, color: C.text }}>{cap}</div>
                  <div style={{ fontSize: 13, color: twe.startsWith('✓') ? C.green : C.gold, textAlign: 'center', fontWeight: 700 }}>{twe}</div>
                  <div style={{ fontSize: 13, color: ac.startsWith('✗') ? C.red : C.muted, textAlign: 'center' }}>{ac}</div>
                </div>
              ))}
            </div>
            <div style={{ marginTop: 24, padding: 24, background: `linear-gradient(135deg, ${C.gold}18, ${C.gold}08)`, border: `1px solid ${C.gold}44`, borderRadius: 12, textAlign: 'center' }}>
              <div style={{ fontSize: 20, fontWeight: 700, color: C.gold, marginBottom: 8 }}>The Verdict</div>
              <div style={{ fontSize: 14, color: C.text, lineHeight: 1.8, maxWidth: 600, margin: '0 auto' }}>Autocab dispatches rides. TruckWithEase runs an entire freight operation — hiring, compliance, payroll, safety, dispatch, and training — autonomously, for less than Autocab charges for dispatch alone. This is not a comparison. It's a category.</div>
            </div>
          </div>
        )}
      </div>

      <style>{`
        @keyframes ticker { from { transform: translateX(0); } to { transform: translateX(-50%); } }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(-8px); } to { opacity: 1; transform: translateY(0); } }
      `}</style>
    </div>
  );
}
